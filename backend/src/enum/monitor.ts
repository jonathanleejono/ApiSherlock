export enum MonitorIntervalScheduleOptions {
  WEEKLY = "weekly",
  DAILY = "daily",
  HOURLY = "hourly",
  MINUTES = "minutes",
}

export enum MonitorScheduleTypeOptions {
  INTERVAL = "Interval",
  DATE = "Date",
}

export enum MonitorDateAMOrPMOptions {
  AM = "AM",
  PM = "PM",
}

export enum MonitorSettingOptions {
  ON = "ON",
  OFF = "OFF",
}

export enum MonitorDateDayOfWeekOptions {
  Sunday = 0,
  Monday = 1,
  Tuesday = 2,
  Wednesday = 3,
  Thursday = 4,
  Friday = 5,
  Saturday = 6,
}
